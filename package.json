{
  "name": "status-board",
  "version": "0.1.0",
  "repository": "https://github.com/mckelvey/status-board",
  "private": true,
  "license": "BSD-3-Clause",
  "dependencies": {
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "devDependencies": {
    "husky": "^0.14.1",
    "lint-staged": "^4.0.0",
    "prettier": "^1.5.0",
    "react-scripts": "1.0.7",
    "react-snapshot": "^1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && react-snapshot",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "src/**/*.{js,jsx}": [
      "prettier --single-quote --write",
      "git add"
    ]
  }
}
